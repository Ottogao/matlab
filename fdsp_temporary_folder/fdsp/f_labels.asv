function f_labels (caption,xaxis,yaxis,zaxis)

%F_LABELS: Add title and axis labels to current plot 
%
% Usage: f_labels (caption,xaxis,yaxis,zaxis)
%
% Inputs: caption = figure title
%         xaxis   = x axis label
%         yaxis   = y axis label
%         zaxis   = optional z axis label
%
% See also: F_WAIT

title (caption)
xlabel (xaxis)
ylabel (yaxis)
if (nargin > 3)
   zlabel (zaxis)
end


